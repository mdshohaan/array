/**
 * @param {Array} arr
 */

function generateID(arr) {
  if (arr.length === o) return 1;
  return arr[arr.length - 1].id + 1;
}
/**
 * @property todoList
 * @method addItem
 * @method update
 * @method done
 * @method next
 * @method find
 * @method list
 */
