// Includes,IndexOf,LastIndexOf

const arr = [1, 2, 3, 4, 6];

// Includes only return True or False
console.log(arr.includes(5)); // false

// IndexOf
console.log(arr.indexOf(3)); // 2

// LastIndexOf
console.log(arr.lastIndexOf(6)); // 4
